(function () {
    "use strict"

    WinJS.UI.Pages.define("/pages/radar/radar.html", {
        ready: function (element, options) {
        },

        updateLayout: function (element, viewState, lastViewState) {
        },

        unload: function () {
        }
    });
})();